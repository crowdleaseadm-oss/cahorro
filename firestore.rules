rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Funciones de utilidad
    function isSignedIn() {
      return request.auth != null;
    }

    // REGLAS PARA CONSULTAS DE GRUPO (Collection Group)
    // Se definen con {path=**} para que coincidan en cualquier profundidad.
    match /{path=**}/saving_circle_memberships/{membershipId} {
      allow read, write: if isSignedIn();
    }

    match /{path=**}/bids/{bidId} {
      allow read, write: if isSignedIn();
    }

    // Perfiles de Usuario
    match /users/{userId} {
      allow read: if isSignedIn();
      allow create, update: if isSignedIn() && request.auth.uid == userId;
      allow delete: if false;

      // Regla local para acceso directo
      match /saving_circle_memberships/{membershipId} {
        allow read, write: if isSignedIn();
      }
    }

    // Círculos de Ahorro
    match /saving_circles/{circleId} {
      allow read: if true;
      allow create, update, delete: if isSignedIn();

      match /bids/{bidId} {
        allow read, write: if isSignedIn();
      }
    }
  }
}